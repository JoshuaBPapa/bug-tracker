import React from 'react';

const Pagination = () => (
  <div>
    <button>prev</button>
    <span>page number out of total pages</span>
    <button>next</button>
  </div>
);

export default Pagination;